# Write your MySQL query statement below
SELECT class FROM (SELECT class, COUNT(*) as student_count
FROM Courses
GROUP BY class) AS P
WHERE p.student_count >= 5

OR

SELECT class
FROM Courses
GROUP BY class 
HAVING p.student_count >= 5
